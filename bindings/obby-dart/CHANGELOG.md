## 0.2.0

- A method per command: `join`, `sendMessage`, `setTyping` and the rest, with `TypingState`.
- `connected` and `disconnected` are now `handleConnected` and `handleDisconnected`.
- Commands carry a `type` tag, matching every other binding.

## 0.1.3

## 0.1.2

## 0.1.1

- Publish through the release workflow.

## 0.1.0

- First release.
